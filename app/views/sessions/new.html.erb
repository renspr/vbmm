<div class="container">
  <div class="row">
    <div class="span12">
      <h1>Anmelden</h1>

      <div class="alert alert-info">
        Melde Dich mit den gleichen Zugangsdaten (Name und Passwort) an, die Du auch für
        <%= link_to Setting['forum_name'], Setting['forum_url'] %> verwendest. Die Anmeldung
        wird an das Forum weitergereicht, dein Passwort wird hier nicht gespeichert.
      </div>

      <%= render 'alerts' %>
      <%= render 'form' %>
    </div>
  </div>
</div>